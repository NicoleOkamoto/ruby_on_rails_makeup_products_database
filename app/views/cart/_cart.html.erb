<%= turbo_frame_tag "cart" do%>
  <h1>Shopping Cart</h1>
  <% if @cart.orderables.count > 0 %>
    <%= render partial: "cart/orderables" %>
    <p>Total: <%= @cart.total %></p>
    <%else%>
    <p>Your cart is empty.</p>
  <%end%>
<%end %>

